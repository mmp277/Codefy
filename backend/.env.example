# Backend Configuration
PORT=3001
DB=<Your MongoDB connection string>
ACCESS_TOKEN_SECRET=<Your secure access token secret>
REFRESH_TOKEN_SECRET=<Your secure refresh token secret>
REFRESH_TOKEN_EXPIRY=300s  # Refresh token expiry time (e.g., 300s = 5 minutes)
ACCESS_TOKEN_EXPIRY=300s   # Access token expiry time (e.g., 300s = 5 minutes)

# CORS Configuration (Frontend URL)
CORS_ORIGIN=http://localhost:3000  # Replace with your frontend URL

# Email Configuration (for notifications/password reset)
EMAIL=<Your email for sending automated emails>
EMAIL_PASSWORD=<Your email app password (not your actual email password)>